# SQLite للتطوير:
DATABASE_URL="file:./dev.db"
# PostgreSQL للإنتاج (غيّر provider في prisma/schema.prisma إلى postgresql):
# DATABASE_URL="postgresql://user:pass@localhost:5432/almaqsd"

# مفتاح توقيع الجلسات — غيّره في الإنتاج
SESSION_SECRET="change-me-to-a-long-random-string"
